name: exchangerates
description: A new Flutter application.

version: 0.0.2+2

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dio: ^5.1.1
  flag: ^6.0.0
  intl: ^0.18.1
  url_launcher: ^6.1.10

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
